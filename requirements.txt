# Star Command - Telescope Controller Requirements
# Python 3.7+

# Optional GUI theme support (recommended)
ttkbootstrap>=1.10.0

# No other external dependencies!
# tkinter is included with Python (required for GUI)
# CLI version has zero dependencies (pure Python stdlib)
